{
  "version": "6.8.1",
  "name": "We Met v6.8.1 One-Tap Portal Stability",
  "changes": [
    "Removes service-worker forced reloads that could swallow the first tap",
    "Keeps valid sessions during temporary network or server failures",
    "Signs out expired or wrong-role sessions cleanly without reload loops",
    "Adds a stable same-origin /listener/ portal route alongside /admin/",
    "Retires the final manual-UPI public flags and stale payment-policy text"
  ]
}
