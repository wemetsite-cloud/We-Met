{
  "name": "we-met-platform",
  "version": "5.17.0",
  "private": true,
  "description": "Publish-ready customer, listener and admin PWAs with QR-only direct UPI checkout, verified wallet credits, Razorpay-ready payments, Malayalam browser calls and secure recovery.",
  "scripts": {
    "install:backend": "npm --prefix backend ci --registry=https://registry.npmjs.org/",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "db:init": "npm --prefix backend run db:init",
    "check": "node tools/check-project.js",
    "test": "npm --prefix backend test"
  },
  "engines": {
    "node": ">=20"
  }
}
