{
  "name": "we-met-platform",
  "version": "8.9.23",
  "private": true,
  "description": "We Met phone-first platform with listener verification, recurring exclusive memberships, private posts and messaging, paid browser calls and audited operations.",
  "scripts": {
    "install:backend": "npm --prefix backend ci --registry=https://registry.npmjs.org/",
    "start": "npm --prefix backend run start:production",
    "dev": "npm --prefix backend run db:init && npm --prefix backend run dev",
    "db:init": "npm --prefix backend run db:init",
    "check": "npm --prefix backend run check",
    "test": "npm --prefix backend test"
  },
  "engines": {
    "node": ">=20"
  }
}
