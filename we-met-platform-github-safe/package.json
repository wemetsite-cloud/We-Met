{
  "name": "we-met-platform",
  "version": "5.0.0",
  "private": true,
  "description": "Publish-ready customer, listener and admin PWAs with Malayalam browser calls, reliable chat, secure recovery and manual UPI proof approval.",
  "scripts": {
    "install:backend": "npm --prefix backend install --registry=https://registry.npmjs.org/",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "db:init": "npm --prefix backend run db:init",
    "check": "node tools/check-project.js"
  },
  "engines": {
    "node": ">=20"
  }
}
